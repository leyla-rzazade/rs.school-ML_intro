#https://www.codewars.com/kata/5467e4d82edf8bbf40000155
#sorting digits of number in descending order
def descending_order(num):
    res = list(str(num)) 
    sorted_list = sorted(res)
    reversed_list = sorted_list[::-1]
    str1 = ''.join(reversed_list)
    return(int(str1))
